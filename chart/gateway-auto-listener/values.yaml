replicaCount: 1

image:
  repository: ghcr.io/itsh-cloud/gateway-auto-listener
  tag: ""  # defaults to .Chart.AppVersion
  pullPolicy: IfNotPresent

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

gateway:
  name: default
  namespace: nginx-gateway

# Hostname validation gates which hostnames a route in a namespace is allowed to claim.
# Strongly recommended in multi-tenant clusters. Default enabled as of chart v0.2.0;
# set enabled=false explicitly if you trust every namespace to claim any hostname.
hostnameValidation:
  enabled: true
  namespacePrefix: "tenant-"
  domainSuffix: ""
  hostnamesAnnotation: "gateway-auto-listener/allowed-hostnames"

metrics:
  enabled: true
  bindAddress: ":8080"
  serviceMonitor:
    enabled: false
    interval: 30s

resources:
  limits:
    cpu: 100m
    memory: 64Mi
  requests:
    cpu: 10m
    memory: 32Mi

podDisruptionBudget:
  enabled: false
  maxUnavailable: 1

serviceAccount:
  create: true
  name: ""
  annotations: {}

nodeSelector: {}
tolerations: []
affinity: {}
podAnnotations: {}
podLabels: {}
priorityClassName: ""

podSecurityContext:
  runAsNonRoot: true
  seccompProfile:
    type: RuntimeDefault

securityContext:
  allowPrivilegeEscalation: false
  capabilities:
    drop: ["ALL"]
  readOnlyRootFilesystem: true
